This repository is built on official ```postgres``` docker image but adds simple ssl init script for SSL secured connections. 
Keep in mind that **this image is only used for dev & test environment**.